# Roadmap — ask-the-board

Seeded at eng-flow wiring time (2026-07-10). Add items as work is planned;
each shipped feature should leave a spec in `product/specs/` and, where a
design decision was made, an ADR in `decisions/`.

## Shipped (pre-wiring)

- Core engine: `create → resolve → score` board-minute lifecycle with per-seat
  Brier/calibration scoring (see README + `examples/`).
- PyPI packaging, CI, landing page/docs.

## Planned

*(none registered yet — this file is the intake point)*
